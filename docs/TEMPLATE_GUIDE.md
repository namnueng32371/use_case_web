# คู่มือ AI Use Case Template v2

ไฟล์: `docs/AI_UseCase_Template_v2.xlsx`

## ภาพรวม: ข้อมูลไหลยังไง

```
sheet 1 (คนกรอก)  ──►  sheet 3 (คะแนน)   ──►  ตัดสินใจว่าทำอันไหนก่อน
     │
     └──ลิงก์ชื่อ──►  sheet 2 (คนกรอก)  ──►  กำหนด Spec Hardware + ราคา
```

**กฎที่ต้องเข้าใจก่อน:** sheet 2 ไม่มีผลกับคะแนนเลยแม้แต่ช่องเดียว
คะแนนมาจาก sheet 1 ล้วนๆ ส่วน sheet 2 ตอบคนละคำถามคือ "ถ้าจะทำ ต้องซื้อเครื่องแบบไหน"

สูตรคะแนน:

```
คะแนน = ( Impact×35% + Feasibility×25% + Data×25% + (6−Risk)×15% ) × 20
ระดับ:  สูง ≥ 75  |  กลาง 50–74  |  ต่ำ < 50
```

ช่วงคะแนนจริงคือ **20–100** ไม่ใช่ 0–100 — กรอกแย่ที่สุดทุกช่องยังได้ 20
(`(1×0.35 + 1×0.25 + 1×0.25 + 1×0.15) × 20 = 20`)

`6 − Risk` คือการกลับด้าน เพราะความเสี่ยงสูง = คะแนนควรต่ำ
ส่วน `× 20` คือการขยายสเกลจาก 1–5 ให้เป็น 20–100 ที่คนอ่านเข้าใจง่ายกว่า

---

## Sheet 1 : `1-UseCase ทั้งหมด` (23 คอลัมน์)

### กลุ่มที่เข้าสูตรคะแนนโดยตรง — 10 ช่อง

| คอลัมน์ | ชื่อ | เป็นคะแนนอะไร | วิธีแปลง |
|---|---|---|---|
| **I** | เวลาที่ใช้กับงานนี้ตอนนี้ (ยังไม่ใช้ AI) | Impact | dropdown 5 ช่วง: <4 ชม.=1 · 4–10=2 · 10–20=3 · 20–40=4 · >40=5 |
| **J** | มูลค่าความเสียหาย/ปี | Impact | dropdown 5 ช่วง: ประเมินไม่ได้=ไม่คิด · <1แสน=2 · 1–5แสน=3 · 5แสน–2ล้าน=4 · >2ล้าน=5 |
| **L** | ตอนนี้ข้อมูลเก็บอยู่ในรูปแบบไหน | Data | ฐานข้อมูล=5 · Excel=4 · รูป=3 · เสียง=2 · กระดาษ=1 · ยังไม่มี=1 |
| **M** | มีข้อมูลย้อนหลังนานแค่ไหน | Data | >3ปี=5 · 1–3ปี=4 · <1ปี=2 · ยังไม่มี=1 |
| **O** | สิทธิ์นำข้อมูลมาใช้ | Data | อยู่ในมือแผนกเราเอง=5 · ต้องขออนุมัติจากแผนกอื่น/IT=3 · เป็นของลูกค้า/vendor หรือยังไม่แน่ใจ=1 |
| **P** | ต้องเชื่อมต่อกับระบบไอทีเดิมมั้ย | Feasibility | ไม่ต้องยุ่งกับระบบไหน=5 · แค่ export ไฟล์ก็พอ=3 · ยังไม่แน่ใจ=2 · ต้องอ่าน/เขียนในระบบโดยตรง=1 |
| **Q** | มีคนดูแลระบบต่อหลังส่งมอบมั้ย | Feasibility | มีคนในแผนก=5 · ยังไม่แน่ใจ=3 · ต้องพึ่งคนนอกตลอด=1 |
| **R** | ถ้า AI ตอบผิด จะเกิดอะไรขึ้น | Risk | ของเสียหลุดถึงลูกค้า/อันตราย/ผิดกฎหมาย=5 · เสียเวลาทำใหม่=3 · แก้ได้ทันที=1 |
| **S** | ต้องให้คนตรวจงานที่ AI ทำซ้ำมั้ย | Risk | ตรวจทุกครั้ง=4 · สุ่มตรวจ=2 · ไม่ต้องตรวจ=1 |
| **T** | ข้อมูลชุดนี้ลับแค่ไหน | Risk **+ Hardware** | ลับมาก (PDPA)=5 · ลับ=4 · ภายใน=2 · สาธารณะ=1 |

### คอลัมน์ P — ความหมายของแต่ละระดับ

ถามว่า **AI ที่จะสร้างต้องไปยุ่งกับระบบไอทีเดิมมากแค่ไหน** ไม่ได้วัดที่ความยากทางเทคนิค
อย่างเดียว แต่วัดที่ **ต้องพึ่งคนอื่น (IT / vendor) แค่ไหน** — ซึ่งเป็นสาเหตุที่โปรเจกต์ AI
ในโรงงานส่วนใหญ่ล่ม ไม่ใช่เพราะ AI ไม่แม่น แต่เพราะรอเชื่อมระบบไม่ได้สักที

| ตัวเลือก | ตัวอย่าง |
|---|---|
| ไม่ต้องยุ่งกับระบบไหน | ติดกล้องใหม่ที่สายพาน AI โชว์ผลบนจอของมันเอง |
| แค่ export ไฟล์ก็พอ | ดึงรายงานจาก ERP เป็น Excel ทุกเช้า → AI วิเคราะห์ → ส่งผลเป็นไฟล์ |
| ต้องอ่าน/เขียนในระบบโดยตรง | AI ตรวจเสร็จต้องเขียนผลเข้า MES ทันทีเพื่อสั่งหยุดสายพาน |

**วิธีเลือก:** ถามว่า *"ถ้า export เป็น Excel มาให้ AI ทุกเช้า แล้วเอาผลไปพิมพ์ใส่ระบบเองทีหลัง
งานนี้ยังใช้ได้มั้ย"* — ถ้าใช้ได้ = เลือกตัวกลาง ถ้าต้องทันทีอัตโนมัติเท่านั้น = เลือกตัวล่าง

> **T เป็นช่องเดียวที่ถูกใช้ 2 ที่** — ทั้งคำนวณ Risk และกำหนดว่าต้องเข้ารหัส
> จำกัดสิทธิ์ และเก็บ log การเข้าถึงระดับไหน

### ข้อกำหนดของโครงการ : Server ตั้งในบริษัทที่เดียว ไม่ใช้ Cloud ทุกกรณี

คำถามเรื่องที่ตั้งจึงไม่ใช่ "ในหรือนอกบริษัท" แต่เป็น **"รวมศูนย์ที่ server กลาง
หรือต้องมีเครื่องแยกที่หน้างาน"** — งาน Real-time ที่ตรวจชิ้นงานบนสายพานมักต้องมี
เครื่องติดข้างเครื่องจักร ไม่ใช่ส่งรูปวิ่งข้ามเครือข่ายไป server กลางแล้วรอผลกลับ

ผลที่ตามมาคือวิธีคิดราคาเปลี่ยนไปด้วย — ไม่ใช่ "บวกราคาเครื่องของแต่ละ use case"
แต่เป็น **"เครื่องเดียวที่จะซื้อ ต้องแรงแค่ไหนถึงรองรับงานที่จะทำจริงและขยายต่อได้"**
งานข้อความหลายอันโหลดโมเดลตัวเดียวแล้วเรียกใช้ร่วมกันได้ ประหยัดกว่าซื้อแยกมาก

**O ไม่ใช่ "มีข้อมูลมั้ย" แต่คือ "เอามาใช้ได้จริงมั้ย"** — เจอบ่อยว่ามีข้อมูลครบแต่ใช้ไม่ได้
เพราะเป็นของลูกค้าที่มี NDA หรืออยู่ในเครื่องจักรที่ vendor ล็อกไว้ ถ้าไม่ถามช่องนี้
คะแนน Data จะออกมาสูงลิ่วแล้วไปตายตอนเริ่มโปรเจกต์จริง

### ช่องบังคับ 10 ช่อง

**I, J, L, M, O, P, Q, R, S, T** (= 10 ช่องที่คิดคะแนนพอดี) ต้องกรอกครบ ระบบถึงจะคำนวณให้
ถ้าขาดแม้ช่องเดียว sheet 3 จะขึ้น **"ข้อมูลไม่ครบ"** แทนคะแนน และไม่เข้าการจัดอันดับ

ถ้าตีมูลค่าความเสียหายเป็นเงินไม่ได้ ให้เลือก **"ประเมินเป็นเงินไม่ได้"**
ระบบจะใช้เฉพาะชั่วโมงคิด Impact — **ไม่ได้หักคะแนน** (ในตารางจะขึ้นเป็น `-`)

### กลุ่มที่ไม่เข้าสูตร แต่ AI อ่านตอนวิเคราะห์ — 5 ช่อง

| คอลัมน์ | ชื่อ | ทำไมต้องมี |
|---|---|---|
| **F** | ปัญหาที่ต้องการแก้ | AI ใช้ทบทวนว่าคะแนนสมเหตุสมผลมั้ย และแนะนำประเภท AI ที่เหมาะ |
| **G** | วิธีการทำงานปัจจุบัน (*Existing Work*) | AI ใช้เข้าใจกระบวนการเดิม เพื่อออกแบบ Flow ใหม่ |
| **H** | แนวทางหลังนำ AI มาใช้ (*Description for Redesign*) | บอกว่าอยากให้ AI ทำอะไร — ใช้เลือกสถาปัตยกรรมและโมเดล |
| **K** | ข้อมูลที่มีอยู่ตอนนี้ (*Data Available*) | AI ตรวจว่า dropdown ที่เลือกตรงกับความจริงมั้ย |
| **N** | ปริมาณข้อมูลโดยประมาณ | มีตัวอย่างพอเทรนมั้ย (4,000 รูป พอ / 50 รูป ไม่พอ) |

**คอลัมน์ N ยังไม่ถูกนำไปคิดคะแนน** เป็นข้อความอิสระให้ AI อ่านอย่างเดียว
หน่วยที่ใช้นับต่างกันตามประเภทงาน จึงมีกล่องคำอธิบายเด้งขึ้นตอนคลิกช่อง:

| ประเภทงาน | นับเป็น |
|---|---|
| ตรวจภาพ | กี่รูป |
| หาสาเหตุปัญหา / NC | กี่เคส กี่ใบ |
| ค้นหา–สรุปเอกสาร | กี่ไฟล์ กี่ฉบับ |
| ทำนายเครื่องเสีย | เคยเสียมากี่ครั้ง |
| วิเคราะห์การผลิต | กี่ล็อต กี่แถวข้อมูล |
| จัดคน / แนะนำ | เคยจัดจริงมากี่ครั้ง |

นับเฉพาะข้อมูลที่**รู้คำตอบแล้ว** (เช่น รูปที่มาร์คแล้วว่าดี/เสีย) —
ข้อมูลดิบที่ยังไม่ได้แยกใช้เทรนไม่ได้

ช่อง F, G, H, K, N ทั้ง 5 ช่องมีกล่องคำอธิบายแบบนี้ เพราะเป็นช่องพิมพ์อิสระที่
คนกรอกมักไม่แน่ใจว่าต้องเขียนละเอียดแค่ไหน

G, H, K คือ 3 คอลัมน์ที่ยกมาตรงจาก Excel template เดิมของ E&E/Prod

### กลุ่มอ้างอิง ไม่คำนวณ — 8 ช่อง

A (No.) · B (ชื่อ) · C (บริษัท) · D (แผนก) · E (Owner) · W (หมายเหตุ)

**U (ความสำคัญที่แผนกอยากได้)** และ **V (ต้องการใช้ภายใน)** — จงใจไม่เอาเข้าสูตร เพราะ
ทุกแผนกจะเลือก "สูง - ต้องการด่วน" หมด ถ้าเอามาคิดก็ไม่ช่วยแยกอะไร
แต่มีประโยชน์ตรงที่เอาไป**เทียบกับคะแนนจริง** — ถ้าแผนกบอกด่วนมากแต่คะแนนออกมา 35
แปลว่าต้องคุยกันว่าทำไมต่างกัน

---

## Sheet 2 : `2-รายละเอียดเทคนิค` (23 คอลัมน์)

A, B, C ลิงก์อัตโนมัติจาก sheet 1 — ที่เหลือทั้งหมดใช้กำหนด Hardware
ตอบคำถาม 3 ข้อ:

### คำถามที่ 1: ต้องใช้ GPU มั้ย (ตัวคูณราคาที่ใหญ่ที่สุด)

| คอลัมน์ | ชื่อ | บอกอะไร |
|---|---|---|
| **D** | ประเภทข้อมูล Input | รูป/วิดีโอ = GPU แน่นอน · ตัวเลข/ตาราง = CPU พอ |
| **M** | Latency ที่รับได้ | Real-time = GPU · Batch = CPU พอ แม้เป็นงานภาพ |
| **P** | ความต้องการโมเดล | Fine-tune = ต้องมี GPU ตอนเทรนด้วย ไม่ใช่แค่ตอนใช้ |

> **D × M คือคู่ที่ตัดสินใจ** — *รูปภาพ + Real-time* = ต้อง GPU
> แต่ *รูปภาพ + Batch* = รันกลางคืนด้วย CPU ได้ ประหยัดหลายแสน

### คำถามที่ 2: ต้องใหญ่แค่ไหน

| คอลัมน์ | ชื่อ | บอกอะไร |
|---|---|---|
| **F** | **Peak concurrent** | **สำคัญที่สุดในกลุ่มนี้** — ตัดสินจำนวน GPU/เครื่อง (100 คน/วันกระจายทั้งวัน ≠ 100 คนพร้อมกันตอน 9 โมง) |
| **E** | Users/วัน | ประมาณ load รวม |
| **G** | Requests/วัน | คำนวณ throughput |
| **J** | จำนวนรูป/วัน | งานภาพ: GPU ตัวเดียวพอมั้ย |
| **K** | ความละเอียดรูป | ตัดสินขนาด VRAM |
| **L** | ภาษาที่ต้องการ | ไทยต้องใช้โมเดลใหญ่กว่า → RAM/VRAM มากกว่า |
| **N** | Availability | 24×7 = ต้องมีเครื่องสำรอง (ราคา ×2) |

E, F, G, H, I, J เป็น dropdown แบบช่วง ไม่ใช่ช่องพิมพ์ตัวเลข — เพราะการตั้ง spec
ดูที่ **ลำดับขนาด** ไม่ใช่ตัวเลขเป๊ะ (10 กับ 12 users ไม่ต่างกัน แต่ 10 กับ 1,000 ต่างกันมาก)

ช่อง **J ถามจำนวนกล้อง/จุดตรวจที่ทำงานพร้อมกัน ไม่ใช่จำนวนรูปต่อวัน** เพราะงาน Real-time
สเปกขึ้นกับจำนวนสายที่ต้องประมวลผลขนานกัน — 600 รูป/วันจากกล้อง 1 ตัวใช้ GPU ตัวเดียว
แต่ 600 รูป/วันจากกล้อง 6 ตัวที่ตรวจพร้อมกันต้องการ 6 สาย inference คนละสเปกเลย

### คำถามที่ 3: เก็บเท่าไหร่ / ตั้งที่ไหน

| คอลัมน์ | ชื่อ | บอกอะไร |
|---|---|---|
| **H** | ขนาด Input/วัน (GB) | Storage ที่โตขึ้นเรื่อยๆ |
| **I** | ขนาดคลังเอกสารทั้งหมด (GB) | งาน RAG: ขนาด RAM + Vector DB (คนละตัวกับ H) |
| **O** | ติดตั้งแบบไหน | server กลาง หรือต้องมีเครื่องที่หน้างาน (ติดข้างเครื่องจักร) — คนละสถาปัตยกรรม คนละงบ |
| **Q** | ต้องเทรนโมเดลใหม่บ่อยแค่ไหน | ครั้งเดียวจบ = เช่า GPU ชั่วคราวพอ · ทุกเดือน = ต้องมี GPU สำหรับเทรนแยก |
| **R** | มีเครื่อง/GPU เดิมใช้ได้มั้ย | **ตัวชี้ขาดว่าต้องซื้อใหม่ หรือไม่ต้องจ่ายเลย** |
| **S** | แชร์เครื่องกับงานอื่นได้มั้ย | งานที่แชร์ไม่ได้ = ต้องนับเป็นเครื่อง/GPU แยกอีกตัว |
| **T** | ต้องเก็บข้อมูลย้อนหลังกี่ปี | คูณกับขนาด Input/วัน = storage ที่ต้องซื้อจริง |
| **Q** | งบประมาณที่ตั้งไว้ | กรอบที่ต้องออกแบบให้อยู่ใน |
| **R** | ข้อจำกัดอื่นๆ | เช่น "พื้นที่ติดกล้องจำกัด" |

S = ผู้ให้ข้อมูลเทคนิค (อ้างอิงเฉยๆ)

---

## Sheet 3 : `3-คะแนนและลำดับ` (21 คอลัมน์)

ไม่ต้องกรอกอะไร ทุกช่องเป็นสูตร อ่านจาก sheet 1 อย่างเดียว

| คอลัมน์ | แสดงอะไร |
|---|---|
| A–C | No. / ชื่อ / แผนก (ลิงก์) |
| D–F | ชม.→คะแนน · มูลค่า→คะแนน · **Impact** |
| G–J | รูปแบบ · ย้อนหลัง · สิทธิ์ · **Data** |
| K–M | ระบบเดิม · คนดูแล · **Feasibility** |
| N–Q | AI ผิด · ตรวจซ้ำ · ความลับ · **Risk** |
| R–T | **คะแนนรวม · ระดับ · อันดับ** |
| U | **ช่องบังคับที่ยังไม่ได้กรอก** (0 = ครบ) |

### เมื่อกรอกไม่ครบ

| กรณี | คะแนนรวม | ระดับ | อันดับ | ช่องที่ขาด |
|---|---|---|---|---|
| ครบ | 89.0 | สูง | 1 | 0 |
| ขาด 2 ช่อง | *(ว่าง)* | **ข้อมูลไม่ครบ** | *(ว่าง)* | **2** |
| ยังไม่ได้กรอก Use Case | *(ว่าง)* | *(ว่าง)* | *(ว่าง)* | *(ว่าง)* |

ช่อง "ระดับ" และ "ช่องที่ขาด" ขึ้นพื้นแดงอัตโนมัติ · คะแนนย่อยที่กรอกไปแล้วยังแสดงปกติ ·
กรอกครบเมื่อไหร่คะแนนขึ้นเอง ไม่ต้องกดอะไร

แสดงคะแนนย่อยทุกตัว เพราะเวลาผู้บริหารถาม *"ทำไมอันนี้ได้ 45"*
จะชี้ได้ทันทีว่า "เพราะข้อมูลยังเป็นกระดาษ (Data=1) และขอใช้ข้อมูลไม่ได้"

คะแนนรวมไล่สีอัตโนมัติ: เขียว ≥75 · เหลือง 50–74 · เทา <50
แถวที่ 6 เป็น Use Case ตัวอย่าง (Auto Inspection = 89.0) ไม่นับเข้าการจัดอันดับ

---

## Sheet DATA : ตัวควบคุมทั้งหมด

| ตำแหน่ง | เนื้อหา |
|---|---|
| A–Y แถว 3–9 | รายการ dropdown 25 ชุด — เพิ่มตัวเลือกได้ ทุก sheet เปลี่ยนตาม |
| A–Y แถว 29–35 | ตารางแปลงตัวเลือก → คะแนน (`-` = ไม่นำมาคิดคะแนน ไม่ใช่ได้ 0) |
| **AB–AC แถว 4–7** | **น้ำหนัก 4 ปัจจัย** (35/25/25/15%) |
| **AB–AC แถว 10–11** | **เกณฑ์แบ่งระดับ** (75 / 50) |

ตัวเลขสีน้ำเงินทุกตัวคือค่าที่ปรับได้ — แก้แล้วคะแนนทุก Use Case ขยับทันที
โดยไม่ต้องแก้สูตร

---

## สรุปสั้นที่สุด

| | คำนวณคะแนน | คำนวณ Hardware |
|---|---|---|
| มาจาก sheet ไหน | Sheet 1 เท่านั้น | Sheet 2 เป็นหลัก |
| กี่ช่อง | 10 ช่อง (I, J, L, M, O, P, Q, R, S, T) | 15 ช่อง (D–R ใน sheet 2) + T จาก sheet 1 |
| ตอบคำถามว่า | "ควรทำอันไหนก่อน" | "ถ้าทำ ต้องซื้ออะไร ราคาเท่าไหร่" |
| กรอกเมื่อไหร่ | รอบแรก ก่อนตัดสินใจ | กรอกพร้อมกันได้เลย ใช้ตอนขออนุมัติงบ |

**หลักการ:** คะแนนคือตัวช่วยกรองเบื้องต้น ไม่ใช่คำตอบสุดท้าย —
ผู้บริหารควรพิจารณา Use Case อันดับต้นๆ ด้วยตนเองอีกครั้ง

---

## การดูแลไฟล์

ไฟล์ Excel สร้างจากสคริปต์ ไม่ได้แก้มือใน Excel — เวลาจะเปลี่ยนอะไรให้แก้ที่สคริปต์
แล้วรันใหม่ สูตรและ dropdown ทั้งหมดจะถูกสร้างใหม่ให้ครบเหมือนเดิมทุกครั้ง

```bash
python docs/build_template.py && python docs/audit_template.py docs/AI_UseCase_Template_v2.xlsx
```

`audit_template.py` ตรวจ 11 หมวด — ลำดับ sheet, จำนวนตัวเลือกกับคะแนนตรงบรรทัดกัน,
ช่วง dropdown ตรงกับจำนวนตัวเลือก, ค่าในแถวตัวอย่างมีอยู่ในรายการจริง, สูตร sheet 3
อ้างคอลัมน์ sheet 1 ถูกตัว, น้ำหนักรวมได้ 100%, การกันข้อมูลไม่ครบ, ลิงก์ข้ามหน้า
และข้อความที่ขึ้นต้นด้วย `=` ซึ่ง Excel จะตีความผิดเป็นสูตร
