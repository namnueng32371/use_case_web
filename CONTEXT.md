# CONTEXT.md — ภาพรวมโปรเจกต์และบรีฟงาน

ไฟล์นี้เก็บ 2 เรื่องไว้ที่เดียว

- **ส่วนที่ 1** ภาพรวมโปรเจกต์ เป้าหมาย แผนงาน และข้อตกลงที่สรุปกันไปแล้ว
  อ่านก่อนเริ่มงานใหม่ จะได้ไม่ต้องย้อนถามเรื่องที่ตัดสินใจไปแล้ว
- **ส่วนที่ 2** บรีฟสำหรับ AI agent ที่จะไปติดตั้งโปรเจกต์นี้บนเครื่องใหม่

ถ้าเป็นมนุษย์ที่อยากติดตั้งใช้งาน ให้อ่าน [README.md](README.md) แทน
มีข้อมูลชุดเดียวกันแต่เขียนเป็นขั้นตอนทีละขั้น ภาษาง่ายกว่า

---

# ส่วนที่ 1 — ภาพรวมโปรเจกต์

## โปรเจกต์นี้คืออะไร

โปรแกรมคำนวณคะแนนเพื่อ **จัดลำดับความสำคัญของ AI Use Case ว่าควรพัฒนาตัวไหนก่อน**

พอรู้คะแนนและลำดับแล้ว ขั้นต่อไปคือต้องรู้ว่า **ต้องใช้ hardware สเปกไหน**
เพื่อเสนอผู้บริหารว่าควรลงทุนเท่าไหร่

**ข้อกำหนดสำคัญ: Server จะตั้งอยู่ที่เดียวในบริษัท (On-premise) ไม่อนุมัติให้ use case
ไหนไปใช้ Cloud** ดังนั้นการคิดสเปกไม่ใช่การบวกราคาเครื่องของแต่ละ use case
แต่เป็นการตอบว่า *เครื่องเดียวที่จะซื้อ ต้องแรงแค่ไหน*

ส่วนเรื่อง **วิธีนำเสนอ spec ในโปรแกรม ยังไม่สรุป** ค่อยออกแบบอีกที

## จุดประสงค์ 2 ข้อ

1. **เสนอลำดับคะแนน use case อย่างถูกต้อง**
2. **รายงานวิเคราะห์ spec hardware ให้ผู้บริหารรู้ว่าควรลงทุนเท่าไหร่**

### ข้อควรระวังเรื่องการคิดราคา

**ไม่คิดราคาจาก use case ทั้งหมดรวมกัน** เพราะ

- การพัฒนาแต่ละ use case ใช้เวลา ทำพร้อมกันทั้งหมดไม่ได้อยู่แล้ว
- ถ้าซื้อเผื่อทุกอันตั้งแต่แรก **งบบานปลาย** และเหลือ hardware ที่ไม่ได้ใช้จริง
- ความจริงที่จะได้พัฒนาจริงคือ 1-2 โปรเจกต์แรก

แนวทางคือคิดสเปกจาก **use case ที่จะทำจริงในเฟสแรก** แล้วออกแบบให้ขยายต่อได้

## แผนงาน 5 ขั้น

| # | หัวข้อ | สถานะ |
|---|---|---|
| 1 | ตรวจสอบไฟล์ Excel use case template ว่ามีข้อมูลไหนตรงกับ field ในโปรแกรม | เสร็จ |
| 2 | **ออกแบบ template ใหม่ ในส่วนที่ขาดไปใน Excel เก่า เพื่อให้ user กรอกข้อมูลที่ AI วิเคราะห์ได้แม่นยำ** | **กำลังทำอยู่** |
| 3 | พัฒนาโปรแกรมให้เก็บข้อมูลตาม Excel template ล่าสุดให้ถูกต้อง | ยังไม่เริ่ม |
| 4 | ออกแบบการเก็บข้อมูลเพื่อกำหนด spec hardware จาก use case โดยใช้ AI วิเคราะห์ | ยังไม่เริ่ม |
| 5 | ออกแบบรายงานสรุปการวิเคราะห์ด้วย AI | ยังไม่เริ่ม |

ขั้นที่ 5 ประกอบด้วย

- 5.1 แผนงานการดำเนินการ
- 5.2 รายงาน Flow การทำงาน
- 5.3 รายงานการวิเคราะห์ spec hardware

## ข้อตกลงที่สรุปไปแล้ว (อย่าย้อนกลับไปถามใหม่)

### เรื่องคะแนน

- สูตร `คะแนน = (Impact x 35% + Feasibility x 25% + Data x 25% + (6 - Risk) x 15%) x 20`
- ช่วงคะแนนจริงคือ **20-100** ไม่ใช่ 0-100
- ระดับ สูง >= 75 / กลาง 50-74 / ต่ำ < 50
- **เวลา/timeline และความเห็นเรื่องความสำคัญของแผนก จงใจไม่เอาเข้าสูตร**
  เพราะทุกแผนกจะตอบว่าด่วนหมด แต่เก็บไว้เทียบกับคะแนนจริงเพื่อหาจุดที่ไม่ตรงกัน
- คะแนนคือตัวช่วยกรองเบื้องต้น ไม่ใช่คำตอบสุดท้าย
- รายละเอียดเต็มอยู่ใน `docs/SCORING.md`

### เรื่อง Template

- **แยก 2 ชั้น** — ชั้น 1 ข้อมูลสำหรับคิดคะแนน (ทุก use case กรอก)
  ชั้น 2 ข้อมูลเทคนิคสำหรับตั้ง spec (กรอกทุก use case เหมือนกัน)
- **ถามเฉพาะสิ่งที่คนกรอกรู้จริง** คนกรอกคือหัวหน้าแผนก Production/QC/R&D
  ไม่ใช่ IT — ห้ามถามคำถามเชิงโครงสร้างพื้นฐาน (แชร์ GPU ได้มั้ย มีเครื่องเดิมมั้ย)
  ให้ถามผลกระทบต่องานของเขาแทน แล้วทีมส่วนกลางแปลเป็นข้อกำหนดเทคนิคเอง
  ด้วยเหตุนี้ sheet 2 จึงแบ่งเป็นโซน A (แผนกกรอก) กับ โซน B (ทีม AI/IT เติม)
  แยกด้วยสี เหลือง = แผนกกรอก, ฟ้า = ทีม AI/IT, เขียว = ระบบดึงมาให้
- **ทุกช่องที่ต้องแปลงเป็นคะแนนหรือใช้ตั้ง spec ต้องเป็น dropdown ไม่ใช่ข้อความอิสระ**
  เพราะข้อความอิสระทำให้ AI ตีความไม่ตรงกันทุกครั้ง คะแนนแกว่ง
- ช่องตัวเลขที่ต้องเดา (ชั่วโมง มูลค่า จำนวน users ฯลฯ) ใช้ **dropdown แบบช่วง**
  เพราะตัวเลขเป๊ะที่มาจากการเดาไม่ได้แม่นกว่าการเลือกช่วง
- ตัวเลือกต้องเขียนให้เข้าใจได้โดยไม่ต้องมีคนอธิบาย บอกสถานการณ์จริงและผลที่ตามมา
- ถ้าข้อมูลไม่ครบ ระบบจะไม่แสดงคะแนนและไม่จัดอันดับให้ ขึ้นว่า "ข้อมูลไม่ครบ" แทน
- **ข้อมูลในฐานข้อมูลเดิมแปลงมาใช้แทนการให้แผนกกรอกใหม่ไม่ได้** ทดลองแล้วพบว่า
  จาก 10 ช่องบังคับ ฐานข้อมูลเดิมมีให้แค่ 1 ช่อง (ระดับความลับ)
  ใช้ได้แค่เป็นตัวตั้งต้นของช่องบรรยาย (ชื่อ แผนก ปัญหา Existing Work Redesign)

### เรื่อง Hardware

- **Server เป็นเครื่องซื้อใหม่ทั้งหมด** ของเดิมที่บริษัทมีอยู่ไม่พอใช้แล้ว
  จึงไม่ต้องถามว่ามีเครื่อง/GPU เดิมเอามาใช้ได้มั้ย เป็นข้อเท็จจริงที่รู้คำตอบแล้ว
- Server ตั้งที่เดียว On-premise ไม่ใช้ Cloud ทุกกรณี
- **"เครื่องเดียว" = เครื่องจริง 1 ตัว แต่ในเครื่องนั้นมี GPU ได้หลายใบ**
  ตัวที่แย่งกันจริงคือ GPU ไม่ใช่ตัวเครื่อง — ตัวถัง CPU แรม storage ใช้ร่วมกันได้
- GPU 1 ใบแบ่งให้ VM หลายตัวใช้พร้อมกันไม่ได้ ถ้าไม่ซื้อ license เพิ่ม (vGPU/MIG)
  แต่หลายโมเดล/หลาย container บน VM เดียวกัน **แชร์ GPU ใบเดียวกันได้ฟรี** ถ้า VRAM พอ
- แนวทางที่วางไว้ : ซื้อตัวถังที่รองรับ GPU ได้ 4 ใบ แต่ใส่จริง 1-2 ใบก่อน
  เพราะตัวถังกับ Power Supply เปลี่ยนทีหลังไม่ได้ ต้องเผื่อตั้งแต่ซื้อ
- **ยังไม่ได้เก็บข้อมูลห้อง server** (ไฟเหลือกี่ kW / แอร์รับได้อีกเท่าไหร่ / UPS / พื้นที่ rack)
  ซึ่งถ้าห้องรองรับไม่ได้ ต้องบวกค่าปรับปรุงห้องเข้างบด้วย มักถูกลืม
- คำถามที่เหลือจึงไม่ใช่ "ในหรือนอกบริษัท" แต่คือ **"รวมศูนย์ที่ server กลาง
  หรือต้องมีเครื่องแยกที่หน้างาน"** (งาน Real-time ที่สายพานมักต้องมีเครื่องติดข้างเครื่องจักร)
- งานข้อความหลายอัน **โหลดโมเดลตัวเดียวแล้วเรียกใช้ร่วมกันได้** ประหยัดกว่าซื้อแยกมาก
  ช่อง "แชร์เครื่องกับงานอื่นได้มั้ย" จึงสำคัญ
- ตัวชี้ขาดว่าต้องใช้ GPU คือ **ประเภทข้อมูล Input x Latency** —
  รูปภาพ + Real-time ต้องใช้ GPU แต่ รูปภาพ + Batch รันด้วย CPU กลางคืนได้

## ไฟล์สำคัญ

| ไฟล์ | คืออะไร |
|---|---|
| `docs/AI_UseCase_Template_v2.xlsx` | Template ที่แจกให้แผนกกรอก (ของจริงที่ส่งมอบ) |
| `docs/TEMPLATE_GUIDE.md` | คู่มืออธิบายทุกคอลัมน์ว่าใช้ทำอะไร |
| `docs/SCORING.md` | ที่มาของสูตรคะแนนและเหตุผลที่ตัดบางปัจจัยออก |
| `docs/build_template.py` | สคริปต์สร้างไฟล์ Excel |
| `docs/use_cases_source/` | ไฟล์ Excel template เดิมของแต่ละแผนก (ต้นฉบับ) |
| `AI_Use_Case_Program12.html` | หน้าเว็บที่ Flask เสิร์ฟ (ใช้กับ MySQL local) |
| `artifact.html` | เวอร์ชันสำหรับ publish เป็น Claude Artifact |

### วิธีแก้ไข Template

**อย่าแก้มือใน Excel** ให้แก้ที่สคริปต์แล้วรันใหม่ เพราะไฟล์มีสูตร 630 ช่อง
และ dropdown หลายสิบชุดที่ต้องสร้างให้ตรงกันทุกครั้ง

```bash
python docs/build_template.py
```

### สิ่งที่ต้องตรวจทุกครั้งหลังแก้ template

**ต้องเปิดด้วย Excel ตัวจริงยืนยันเสมอ** ไม่ใช่เชื่อการอ่านไฟล์ด้วย Python อย่างเดียว

| เรื่อง | ทำไมต้องตรวจ |
|---|---|
| Excel เปิดได้ ไม่มีหน้าต่างขอซ่อมไฟล์ | ข้อความใน validation ยาวเกิน 32 (หัวกล่อง) / 255 (เนื้อกล่อง) จะทำให้ไฟล์เสีย แต่ openpyxl เขียนออกมาได้เฉยๆ ไม่เตือน |
| dropdown ขึ้นครบทุกช่อง | เคยเจอกรณีช่วงอ้างอิงไม่ตรงกับจำนวนตัวเลือก และกรณีใส่ `=` นำหน้าสูตรแล้ว Excel ตัดทิ้งเงียบๆ |
| ค่าในแถวตัวอย่างต้องมีอยู่ในรายการจริง | ถ้าแก้ตัวเลือกแล้วลืมแก้แถวตัวอย่าง จะกลายเป็นค่าที่เลือกไม่ได้ |
| สูตรใน sheet 3 อ้างคอลัมน์ sheet 1 ถูกตัว | เวลาเพิ่ม/ลบคอลัมน์ ตัวอักษรคอลัมน์จะเลื่อนทั้งแถว |
| น้ำหนักคะแนนรวมกันได้ 100% | |
| ไม่มีข้อความที่ขึ้นต้นด้วย `=` | Excel จะตีความเป็นสูตรแล้วไฟล์พัง |

**ห้ามตรวจไฟล์ที่ผ่านการเปิดด้วย Excel มาแล้ว** — Excel จะย้าย dropdown ไปเก็บใน
รูปแบบ `x14` ซึ่ง openpyxl อ่านไม่เห็น ทำให้เข้าใจผิดว่า dropdown หายไปหมด
ให้ตรวจไฟล์ที่เพิ่งสร้างจากสคริปต์เท่านั้น

## ข้อควรรู้เรื่อง Artifact

มี Artifact 2 ตัว

- **ตัวเก็บถาวร** ชื่อ "AI Use Case Tool (Archived)" — ไม่พัฒนาต่อแล้ว เก็บไว้เฉยๆ
- **ตัวหลัก** ชื่อ "AI Use Case Prioritization Tool" — พัฒนาต่อที่ตัวนี้

เวลา publish ต้องระบุ `url` ของ Artifact ที่ต้องการอัปเดตเสมอ
เพราะการ publish โดยไม่ใส่ url อาจไปทับ Artifact เดิมที่เคย publish ด้วย
path เดียวกันในบทสนทนานั้น (เคยเกิดขึ้นมาแล้ว)

---

# ส่วนที่ 2 — บรีฟการติดตั้งบนเครื่องใหม่ (สำหรับ AI agent)

ส่วนนี้เขียนสำหรับ AI coding agent ที่ต้องไป reproduce การตั้งค่าบนเครื่อง Windows เครื่องใหม่
**ห้ามคิดสถาปัตยกรรมใหม่เอง ให้ทำตามเอกสารนี้**

โปรแกรมเป็นเครื่องมือหน้าเดียว ([AI_Use_Case_Program12.html](AI_Use_Case_Program12.html))
เดิมเก็บข้อมูลใน `localStorage` ของเบราว์เซอร์ (เห็นคนเดียว) ถูกย้ายมาใช้ **MySQL**
เป็น backend กลาง ผ่าน API ที่เขียนด้วย **Flask** ([server.py](server.py))
เพื่อให้หลายคนในวง LAN เดียวกันเห็นข้อมูลชุดเดียวกัน

## งานที่ต้องทำ

ตั้งค่าโปรเจกต์นี้ให้รันได้บนเครื่อง Windows เครื่องใหม่ โดย reproduce การตั้งค่าที่ใช้งานได้จริงตามที่อธิบายไว้ด้านล่างนี้เป๊ะๆ **ห้ามคิดสถาปัตยกรรมใหม่เอง ให้ทำตามเอกสารนี้**

1. ติดตั้ง Python 3.10 ขึ้นไป (ต้องเพิ่มเข้า PATH ด้วย)
2. ติดตั้ง MySQL Server (เวอร์ชัน 8.x/9.x เวอร์ชันใดก็ได้ที่ค่อนข้างใหม่ — **เลขเวอร์ชันไม่แน่นอนในแต่ละเครื่อง ต้อง detect เอง ห้าม hardcode "8.4"**)
3. รัน [setup_mysql.ps1](setup_mysql.ps1) แบบ Administrator สคริปต์นี้จะหาโฟลเดอร์เวอร์ชัน MySQL ที่ติดตั้งจริงใต้ `C:\Program Files\MySQL\` เอง, initialize data directory ด้วย `--initialize-insecure` (root **ไม่มีรหัสผ่าน**), เขียนไฟล์ `my.ini` แบบขั้นต่ำ, และติดตั้ง MySQL เป็น Windows service ชื่อ `MySQL84` (ชื่อนี้เป็นชื่อเก่าที่ตั้งไว้ตอนแรก ไม่จำเป็นต้องตรงกับเวอร์ชันจริง) เพื่อให้รันอัตโนมัติทุกครั้งที่เปิดเครื่อง
4. สร้าง database และตาราง โดยรัน [schema.sql](schema.sql) กับ server (`mysql -u root < schema.sql` **ไม่ต้องใส่ `-p`** เพราะ root ไม่มีรหัสผ่านโดยตั้งใจ ดูเหตุผลด้านล่าง)
5. `pip install -r requirements.txt` (Flask + PyMySQL)
6. `python server.py` — คำสั่งนี้จะเสิร์ฟทั้งหน้าเว็บ HTML และ REST API (`/api/usecases` แบบ GET/PUT) ที่ port `8765` โดยต่อกับ MySQL
7. ตรวจสอบโดยเปิด `http://localhost:8765` แล้วต้องเห็นหน้าเว็บโหลดขึ้นมาพร้อมรายการ use case ว่างเปล่า (ฐานข้อมูลตอนนี้มี 0 แถวโดยตั้งใจ — ข้อมูล mock/ทดสอบถูกล้างออกไปแล้ว **ห้าม seed ข้อมูลกลับเข้าไปเอง** เว้นแต่จะถูกขอให้ทำ)

[start_all.bat](start_all.bat) ช่วย automate ขั้นตอนที่ 6 เป็นต้นไปสำหรับการใช้งานประจำวันหลัง setup เสร็จแล้ว (start MySQL แบบ background process + `server.py` ในดับเบิลคลิกเดียว)

## จุดออกแบบสถาปัตยกรรมที่ต้องรักษาไว้ ห้ามเปลี่ยนเอง

- **MySQL root ไม่มีรหัสผ่าน ในทุกเครื่อง โดยตั้งใจ** เป็นการลดความซับซ้อนแบบตั้งใจสำหรับเครื่องมือที่ใช้ในวง LAN ภายในเท่านั้น — ทุกเครื่องที่รัน `server.py` จะต่อด้วย credential ชุดเดียวกัน (`root` / รหัสผ่านว่าง / `127.0.0.1:3306` / database `ai_usecase_db`) จะได้ไม่ต้องตั้งค่าหรือแชร์รหัสผ่านให้ตรงกัน **ห้ามเพิ่มรหัสผ่านเอง** เว้นแต่ user จะขอชัดเจน — ถ้าขอ ต้องบอกให้ตั้ง environment variable `DB_PASSWORD` (และอาจต้องตั้ง `DB_USER` ด้วย) ก่อนรัน `server.py` เพราะค่า default ใน [server.py](server.py) เป็นค่าว่าง
- **การตั้งค่านี้ใช้ได้เฉพาะในวง LAN เท่านั้น ห้ามเปิดออกอินเทอร์เน็ตเด็ดขาด** ไม่มีระบบ auth ไม่มี TLS ไม่มีรหัสผ่านป้องกัน DB ห้ามแนะนำให้เปิด port 3306 หรือ 8765 ออกสู่อินเทอร์เน็ตสาธารณะ
- **โครงสร้างข้อมูลตั้งใจให้ยืดหยุ่น ไม่ผูก schema ตายตัว** ตาราง `use_cases` มีคอลัมน์ `id`, `name`, `dept` บวกคอลัมน์ `payload` แบบ JSON ที่เก็บ object ของ use case ทั้งหมดไว้ข้างใน ออกแบบให้ตรงกับพฤติกรรมเดิมของ frontend (array ของ JS object ที่ field ไม่ตายตัว บันทึกทั้งก้อน) **อย่า normalize เป็นหลายคอลัมน์เพิ่มเอง** เว้นแต่จะถูกขอ
- **Contract การ save/load ของ frontend เป็นแบบ "แทนที่ทั้งก้อน" ไม่ใช่ CRUD ทีละแถว**: `GET /api/usecases` คืน array ทั้งหมด, `PUT /api/usecases` จะ**แทนที่เนื้อหาทั้งตารางในทีเดียวในหนึ่ง transaction** ตรงกับพฤติกรรมเดิมของ `localStorage` เป๊ะๆ (ฟังก์ชัน `loadData()`/`saveData()` ในไฟล์ HTML อ่าน/เขียนทั้ง list ทีเดียวเสมอ) — ดูที่ [AI_Use_Case_Program12.html:903](AI_Use_Case_Program12.html:903) **ถ้าจะแก้ API ให้รักษา contract นี้ไว้** อย่าเปลี่ยนเป็น endpoint แบบทีละรายการโดยไม่ปรึกษา user ก่อน เพราะต้องแก้ frontend ด้วย
- Frontend ยังมี fallback ไป `localStorage` แบบเงียบๆ ถ้า fetch `/api/usecases` ล้มเหลว (เช่น เปิดไฟล์ HTML ตรงๆ โดยไม่ได้รัน `server.py`) เป็นพฤติกรรมที่ตั้งใจไว้เป็น soft failure ไม่ใช่บั๊ก

## ปัญหาที่เคยเจอมาแล้ว (อย่าทำซ้ำ)

- **ไฟล์ PowerShell script ที่มีตัวอักษรไทยอาจ parse ไม่ผ่าน** Windows PowerShell 5.1 ตรวจจับ UTF-8 ของไฟล์ `.ps1` ที่ไม่มี BOM ได้ไม่แน่นอน การตีความ byte ผิดอาจสร้างตัวอักษรแปลกปลอมที่ทำให้ string literal พังและเกิด syntax error ด้วยเหตุนี้ `setup_mysql.ps1` จึงใช้**ข้อความภาษาอังกฤษล้วน** ถ้าจะแก้ไฟล์นี้ ให้คงเป็น ASCII ล้วน หรือถ้าจำเป็นต้องใช้ตัวอักษรที่ไม่ใช่ ASCII ต้องเพิ่ม UTF-8 BOM เข้าไปด้วย
- **ชื่อโฟลเดอร์เวอร์ชัน MySQL ไม่คงที่** เครื่องต้นทางได้ `MySQL Server 8.4` แต่เครื่องอื่นจะต่างออกไป (เช่น `MySQL Server 8.0`, `9.x`) ต้อง detect เสมอด้วย `Get-ChildItem "C:\Program Files\MySQL" -Directory` ห้าม hardcode เวอร์ชัน
- **การติดตั้ง Windows Service ต้องใช้สิทธิ์ Administrator / UAC elevation** ซึ่งไม่สามารถขอสิทธิ์แบบเงียบๆ จาก shell ที่ไม่ interactive ได้ ถ้าคุณเป็น AI agent ที่ไม่มี shell แบบ elevated ก็จะติดตั้ง service เองไม่ได้ — ต้องส่งคำสั่ง PowerShell ที่ต้องรันให้ user แล้วขอให้เขารันเองในหน้าต่าง PowerShell ที่เปิดแบบ elevated
- **ตัวติดตั้ง MySQL (GUI) จะบังคับให้ตั้งรหัสผ่าน root เป็นค่า default** ให้ข้าม/ปิดหน้านั้นระหว่างติดตั้ง (หรือปล่อยให้ตั้งไปก่อนแล้วค่อย overwrite ทีหลังด้วยการรัน `mysqld --initialize-insecure` ใหม่ใส่ data directory ที่ยังไม่เคยใช้) — อย่าไปสู้กับ GUI ใช้วิธีสคริปต์ใน `setup_mysql.ps1` แทน

## รายการไฟล์และหน้าที่

| ไฟล์ | หน้าที่ |
|---|---|
| `AI_Use_Case_Program12.html` | ตัวแอป frontend (ไฟล์เดียว vanilla JS) |
| `server.py` | Flask backend: เสิร์ฟหน้าเว็บ + REST API `/api/usecases` ที่ต่อกับ MySQL |
| `schema.sql` | สร้าง database `ai_usecase_db` และตาราง `use_cases` |
| `setup_mysql.ps1` | สคริปต์ตั้งค่า/ติดตั้ง MySQL เป็น Windows service แบบไม่มีรหัสผ่านในคลิกเดียว |
| `requirements.txt` | รายชื่อ Python package ที่ต้องใช้ (`Flask`, `PyMySQL`) |
| `start_all.bat` | ตัว launcher สำหรับใช้งานประจำวัน (ดับเบิลคลิกเดียว) |
| `.gitignore` | กันไฟล์ lock ของ Office, `__pycache__`, และไฟล์ backup `*_backup_*.sql` ไม่ให้หลุดเข้า git |
| `README.md` | คู่มือสำหรับมนุษย์ (ใช้งานประจำวัน, ติดตั้งเครื่องใหม่, วิธีใช้ git, troubleshooting) |
| `.claude/launch.json` | บอกเครื่องมือ browser-preview ของ Claude Code ให้รัน `python server.py` |

## เช็คลิสต์ตรวจสอบก่อนถือว่าเสร็จ

หลัง setup เสร็จ ต้องตรวจครบทุกข้อนี้ก่อนประกาศว่าสำเร็จ:

1. `mysqladmin -u root ping` → ต้องขึ้น `mysqld is alive`
2. `SELECT COUNT(*) FROM ai_usecase_db.use_cases;` → รันได้โดยไม่ error (0 แถวถือว่าปกติ/ถูกต้องสำหรับเครื่องที่เพิ่งติดตั้งใหม่)
3. เปิด `http://localhost:8765` แล้วหน้าแอปโหลดขึ้นมาได้
4. ทดสอบ `PUT /api/usecases` ด้วยข้อมูลทดสอบ 1 รายการ แล้ว `GET /api/usecases` ต้องได้ข้อมูลกลับมาตรงกัน จากนั้น**ต้องล้างข้อมูลทดสอบทิ้งด้วย** ห้ามปล่อยข้อมูลทดสอบค้างไว้
5. `git status` ต้องสะอาดถ้ามีการแก้ไฟล์ใดๆ — อย่าปล่อยไฟล์ untracked ค้างไว้แบบไม่ได้ตั้งใจ
